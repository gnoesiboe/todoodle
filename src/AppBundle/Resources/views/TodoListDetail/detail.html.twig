{% extends '::base.html.twig' %}

{% block body %}
    <h1>{{ todoList.token }}</h1>
    <div class="js-app-container" data-id="{{ todoList.id }}" data-token="{{ todoList.token }}">
        Loading..
    </div>
{% endblock %}
